linspace 와 : 연산자는 모두 행벡터임. 열벡터로 바꾸려면? 변수 x = x’으로 재정의 해주면됨

rand 함수: rand(5) = 5x5 난수 행렬 출력 →non 행렬 출력

rand(a,b) = a x b 행렬 출력

zeros 함수: zeros(a,b) = 요소가 모두 영인 a x b 행렬 출력

size(x): x행렬의 크기를 알 수 있음

인덱싱 배열에서 값을 추출하여 수정, 배열 내 값의 위치: 인덱스

x(1,:) → 1행 호출/ x(:,3) → 3열 호출

x(a,b): x의 a행, b열 값 호출

end 키워드: end를 행 또는 열 인덱스로 사용시 마지막 요소를 참조할수 잇음, end-1,end-2 처럼 마지막에서 두번쨰,,,n번재를 산술연산 적용할수 있음

x(1) = a  → 1번째 요소 변경

a = max(x) → x벡터의 최댓값

round(x): 값을 반올림하여 정수로 나타내주는 함수

x = randi(20,5,7) → 1~20 사이 임의의 정수를 5x7 행렬로 나타냄

plot(sample, mass1) → x,y 축 함수를 그려줌

plot(sample,mass2,"r*") → 빨간색과 *을 이용하여 그려줌

hold on
plot(sample,mass2, "ks") → 다른 선을 겹쳐서 그려줌
