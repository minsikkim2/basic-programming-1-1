clear: 작업 공간 초기화

clc: 명령창 초기화

load myData m 변수 m 만 불러옴(myData.mat로부터)

save (파일명) m 변수m 을 (파일명).mat 이라는 새로운 mat 파일로 저장

내장함수 : sin(), sqrt(),cos(), tan()

*삼각함수() 값은 라디안으로 계산되므로 x/180*pi 로 변환해야함*

format long: 소수점 이하 15 자리까지 표시해줌

>> format long

>>x

벡터 만들기 x = [2 4 6] —→ 2  4  6

행벡터 공백, 따옴표/ 열벡터 ;

일정한 간격 벡터 x = 5:8    → x = 5 6 7 8 간격 디폴트값, 바꿔주려면 중간에 간격 입력 ex) x = 1:0.5:8

만들고자 하는 벡터에서 각 요소 간 간격이 아니라 원하는 요소의 개수를 이미 알고 있는 경우
